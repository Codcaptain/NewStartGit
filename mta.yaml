_schema-version: "3.2"
ID: Gittesting
description: git
version: 0.0.1
modules:
- name: Gittesting-destination-content
  type: com.sap.application.content
  requires:
  - name: Gittesting-destination-service
    parameters:
      content-target: true
  - name: Gittesting-repo-host
    parameters:
      service-key:
        name: Gittesting-repo-host-key
  - name: Gittesting-uaa
    parameters:
      service-key:
        name: Gittesting-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: Gittesting_html_repo_host
          ServiceInstanceName: Gittesting-html5-service
          ServiceKeyName: Gittesting-repo-host-key
          sap.cloud.service: Gittesting
        - Authentication: OAuth2UserTokenExchange
          Name: Gittesting_uaa
          ServiceInstanceName: Gittesting-xsuaa-service
          ServiceKeyName: Gittesting-uaa-key
          sap.cloud.service: Gittesting
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: Gittesting-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: Gittesting-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - cominctureproject1.zip
      name: cominctureproject1
      target-path: resources/
- name: cominctureproject1
  type: html5
  path: project1
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: Gittesting-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: Gittesting-destination-service
    service-plan: lite
- name: Gittesting-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: Gittesting-xsuaa-service
    service-plan: application
- name: Gittesting-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: Gittesting-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
